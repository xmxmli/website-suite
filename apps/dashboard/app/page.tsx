import { PlaceholderShell } from "@email-suite/ui";

export default function DashboardPage() {
  return (
    <PlaceholderShell
      eyebrow="Dashboard placeholder"
      title="Dashboard coming later"
      description="The dashboard app exists as a placeholder in this phase. Business features, login, data tables, and settings will be added after the website UI direction is confirmed."
    />
  );
}
